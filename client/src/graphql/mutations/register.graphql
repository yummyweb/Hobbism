mutation Register($email: String!, $password: String!, $firstName: String!, $lastName: String!) {
    register(data: {
      email: $email,
      password: $password,
      firstName: $firstName,
      lastName: $lastName
    }) {
      id
      name
      email
    }
}